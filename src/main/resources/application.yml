server:
  port: ${PORT}


spring:
  application:
    name: Skytouch
  profiles:
    active: prod

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    protocol: ${MAIL_PROTOCOL:smtp}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${MAIL_SMTP_STARTTLS_ENABLE:true}
            required: ${MAIL_SMTP_STARTTLS_REQUIRED:true}
          connectiontimeout: ${MAIL_CONNECTION_TIMEOUT:30000}
          timeout: ${MAIL_TIMEOUT:30000}
          writetimeout: ${MAIL_WRITE_TIMEOUT:30000}

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    url: /v3/api-docs
    disable-swagger-default-url: true

app:
  jwt:
    secret: ${JWT_SECRET}
  admin:
    email: ${ADMIN_EMAIL}
    default-password: ${ADMIN_PASSWORD}
  auth:
    email-from: ${APP_EMAIL_FROM:${MAIL_USERNAME:noreply@skytouch.com}}
    log-otp: ${APP_LOG_OTP:true}
    mail-send-enabled: ${MAIL_SEND_ENABLED:true}
    otp:
      length: 6
      expiration-ms: 600000
      max-attempts: 5
    session:
      expiration-ms: 86400000
  storage:
    cloudinary-enabled: ${CLOUDINARY_ENABLED:true}
    local-upload-dir: ${LOCAL_UPLOAD_DIR:uploads}
  job-alert:
    digest:
      enabled: ${JOB_ALERT_DIGEST_ENABLED:true}
      cron: ${JOB_ALERT_DIGEST_CRON:0 0 8 * * *}
      lookback-hours: ${JOB_ALERT_DIGEST_LOOKBACK_HOURS:24}

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME}
  api-key: ${CLOUDINARY_API_KEY}
  api-secret: ${CLOUDINARY_API_SECRET}

address:
  validation:
    enabled: ${ADDRESS_VALIDATION_ENABLED:false}
    api-key: ${GOOGLE_ADDRESS_API_KEY:}
    endpoint: ${GOOGLE_ADDRESS_VALIDATION_ENDPOINT:https://maps.googleapis.com/maps/api/geocode/json}

paystack:
  secret-key: ${PAYSTACK_SECRET_KEY:sk_test_d00a8a661d4dec8e0eeae18fac6ccfa331bce99c}
  public-key: ${PAYSTACK_PUBLIC_KEY:pk_test_6a1a73b27bfabc495f2566160f295a1e66c93fd0}
  callback-url: ${PAYSTACK_CALLBACK_URL:https://dawnlike-solomon-lightfootedly.ngrok-free.dev/api/v1/webhooks/paystack}
  webhook-url: ${PAYSTACK_WEBHOOK_URL:https://dawnlike-solomon-lightfootedly.ngrok-free.dev/api/v1/webhooks/paystack}
  api-url: https://api.paystack.co
